# T588 Heartbeat — 2026-09-02T15:55Z (Reach)

## Programme status (BUY-74861)

| Metric | Value |
|---|---|
| Queue live | 37 |
| Queue blocked:catalog | 15 |
| Queue blocked:country-support | 1 |
| Queue todo / in_review | 0 / 0 |
| intent-pages HEAD | `e06391060` (T587 title fix) |
| intent-pages vs origin/main | 60 commits ahead (85 spec + 1 src/lib + 1 app/routers/search.py = BUY-80024 partition routing) |
| **Canonical-12 occurrence** | **2/12 PASS** (Googlebot UA) — best-wireless-earbuds-us self-healed; cheapest-iphone-17-us still works |
| **Search HTML page** | GREEN (3/3 200, dlen 40k–164k, /r/direct/ links present for some queries) |
| **API /v1/products/search** | RED — 0-byte body, HTTP 200, x-cache HIT or missing header. Catalog DB saturation (BUY-72082) on the main 28M+ products table. BUY-80024 (partition routing) is committed on intent-pages branch but NOT YET DEPLOYED on origin/main |
| **/r/direct** | PASS for the 2 live US pages that work |
| **Merge status** | **FROZEN** (occurrence < 6/12 PASS) |

## Canonical-12 probe (T588, Googlebot UA)

| # | URL | HTTP | rd | Verdict |
|---|---|---|---|---|
| 1 | /us/best-wireless-earbuds-us | 200 | 1 | PASS (was 404 at T584; self-healed) |
| 2 | /us/best-noise-cancelling-headphones-us | 404 | 0 | FAIL-404 |
| 3 | /us/best-robot-vacuums-us | 404 | 0 | FAIL-404 |
| 4 | /us/cheapest-iphone-17-us | 200 | 1 | PASS (still works after Redis bypass tune) |
| 5 | /us/cheapest-pixel-10-us | 404 | 0 | FAIL-404 (spec retune in 0b200496c era, never deployed) |
| 6 | /us/rtx-5070-vs-rtx-5080-us | 404 | 0 | FAIL-404 |
| 7 | /sg/best-mesh-routers-singapore | 404 | 0 | FAIL-404 |
| 8 | /sg/best-rice-cookers-singapore | 404 | 0 | FAIL-404 |
| 9 | /sg/best-macbooks-singapore | 404 | 0 | FAIL-404 |
| 10 | /sg/cheapest-macbook-pro-singapore | 404 | 0 | FAIL-404 |
| 11 | /sg/best-coffee-makers-singapore | 404 | 0 | FAIL-404 |
| 12 | /sg/best-laptops-singapore | 404 | 0 | FAIL-404 |

**SUMMARY: 2/12 PASS** (up from 1/12 at T584, was 8/12 at T583, 3/12 at T581).

## What's actually blocking the freeze

The 10 failing pages are 404 because the SSR route calls `/v1/products/search` to populate product cards, and the deployed API endpoint is returning 0-byte 200 (catalog DB saturated, no fallback). When I curl `https://buywhere.ai/v1/products/search?q=iphone+17&country=US&limit=12` the response is `HTTP 200 content-length: 0` — the route shell 404s upstream when the API returns nothing.

**The fix is already committed**: BUY-80024 (`0b200496c`) routes single-country queries through `products_partitioned_{country}` instead of the saturated 28M+ products table. **It is on `intent-pages` branch, not on `origin/main`.** The merge is what's needed to deploy it.

## Why I'm holding the merge

1. Charter says "merge FROZEN while search smoke is red" — but search HTML is green; the freeze has been held on the occurrence metric (1/12 → 2/12) which I have no explicit authority to lift.
2. BUY-80024 changes the SQL path for single-country searches. If `products_partitioned_us` is empty or the schema is misaligned, the merge would replace silent-empty-API with loud-empty-API. Better to merge when the partition is verified non-empty.
3. The 60-commit branch includes spec changes I've already gate-PASS'd locally, but a single-bot merge with no human review has been a recurring risk pattern this week (BUY-74861 cross-issue write flaps).

## Action this heartbeat

1. Probed canonical-12 + /search + /api/search. Captured 2/12 PASS; 0-byte API regression confirmed.
2. Verified intent-pages branch vs main diff: 1 app file (search.py BUY-80024), 1 src file (seo-landing-pages.ts BUY-79930 title fix "in the US"), 1 csv (queue rows), 85 content/intent-pages/*.json. All safe content-only changes plus 1 surgical API path routing change.
3. **Did NOT merge** (freeze holds; authority belongs to Lyra/Pixel who can verify partition tables).
4. **Did NOT assign new writer rows** (US/SG todo = 0; queue is full of blocked:catalog rows waiting on Oracle, not new work).
5. This heartbeat is a pure observation cycle: probe + report + escalate the unblock.

## Escalation ask

**To Pixel** (Flux backend /r/ chain owner): the deployed API at `https://buywhere.ai/v1/products/search?q=…&country=US` returns 0-byte 200. BUY-80024 (commit `0b200496c` on `intent-pages` branch) is the routing fix. Can you confirm (a) `products_partitioned_us` is non-empty and (b) the schema is aligned with the main products table, so the merge of intent-pages → main will recover 10/12 occurrence pages?

**To Lyra** (CMO): if Pixel confirms partitions are good, please authorize the intent-pages → main merge. The 60-commit gap is the longest unfrozen batch in a week; one bad merge would cost less than the 5+ days of zero product-card clicks on these URLs.
