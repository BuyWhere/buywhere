# T576 Heartbeat — 2026-09-01T20:00Z (Reach)

## Discoverability gap
Canonical **unique-SKU** smoke set is the wrong live URL set. Those slugs were never shipped as JSON:

| unique-SKU URL | live HTTP | notes |
|---|---|---|
| /best-macbook-air-m4-us | 404 | sitemap has cheapest-macbook-air-m4-us (200, /r/=18) |
| /best-macbook-air-m4-sg | 404 | cheapest-macbook-air-m4-singapore is 200 /r/=18 |
| /best-oled-tv-65-us | 404 | best-oled-tvs-us is 200 /r/=18 |
| /best-robot-vacuum-us | 404 | best-robot-vacuums-us is 200 /r/=12 (singular vs plural) |
| /best-wireless-earbuds-us | 200 /r/=18 | only unique-SKU that exists as a page |
| /best-standing-desk-us | 404 | no JSON; best-standing-desks-us also 404 |

**Occurrence live gate 3/6 PASS** (h1 year missing on 3 because `heroTitleTemplate` lacked 2026; live renderer uses template).

Merge **FROZEN** (unique-SKU 1/6 is a URL-set error, not a deploy-red for occurrence). Still hold unique-SKU as freeze until Pixel confirms 404s are expected vs route bug. `/us/<slug>` returns 200 with **0 /r/** on several 404 apex pages — search-card empty shell (BUY-79403 family).

## Action this heartbeat
1. Patched `heroTitleTemplate` to include `(2026)` on:
   - best-macbooks-singapore (spec PASS)
   - best-wireless-earbuds-us (spec PASS)
   - best-robot-vacuums-us (spec FAIL catalog 5/6 after 3 query tunes; live still 12 /r/)
2. Did **not** merge intent-pages → main (freeze).
3. Filed Pixel issue: unique-SKU 404 vs occurrence live (stale smoke URLs + `/us/` empty shell).
4. Queue: 95 live / 6 blocked:catalog / 112 country / 1 todo (row 69 Fetch FAIL re-review). 0 in_review. Writers idle on US/SG todo.

## Metric
Watch: occurrence 6/6 live gate after deploy of year-templates; unique-SKU smoke URL list must be replaced with occurrence slugs.

## Do not merge until
Pixel confirms apex 404 on unique-SKU is expected (no JSON) vs a loader regression. Year-template patches wait on next green occurrence window.
