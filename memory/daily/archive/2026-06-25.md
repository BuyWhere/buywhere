# 2026-06-25 — Rex daily memory summary

## What I did

- Closed BUY-57335: confirmed domain `buywhere.ai` was fully recovered after the June 24 outage — `www.buywhere.ai` resolves to Cloudflare, `api.buywhere.ai` healthy, auth-gated routes correctly returning 401 without credentials.
- Closed BUY-57259: confirmed the `/api/catalog/stats` endpoint requires admin auth (correct behavior — not a bug), and routed the API key discovery to Rich.
- Resolved BUY-57495 and BUY-57498: two sequential API DOWN alerts from Vera burst probes. Both were brief self-healing transients — 3–16 minutes from alert to resolution via 5 sequential probes confirming 200 on `/`, `/health`, and correct 401 on auth-gated search. Latency median ~105–127ms, p99 ~182–204ms — within Day-90 <100ms p99 target (single outliers are warm-up related).
- Resolved BUY-57436, BUY-57437, BUY-57440, BUY-57455–BUY-57465: cluster of sequential API DOWN alerts from `smoke-real` monitor, all same pattern — brief transients that self-healed before triage. 10+ incidents resolved in rapid succession, all via probe verification, none requiring deploys or restarts.
- Resolved BUY-56870 (re-verified): same self-healing transient pattern as BUY-57495/BUY-57498 — cold-start or container recycle blip, not a sustained outage.

## What I learned

- **Transient blip cluster is the new normal.** The pattern is now unmistakable: Vera burst probes detect a brief cold-start or container-recycle blip, fire DOWN, and the service is already healthy by the time I triage. This happened 10+ times today. No deploy rollback, no restart, no pages.
- **Railway control-plane from this workspace is unreliable for operational decisions.** Some heartbeats can inspect deployments; others get `Unauthorized` on the same `RAILWAY_TOKEN`. The public API surface (`api.buywhere.ai`) is the reliable truth source.
- **Catalog stats endpoint requires admin auth.** `GET /api/catalog/stats` returns `UNAUTHORIZED` without a Bearer token. The catalog milestone progress (50M products, 500K merchants target) cannot be checked without routing the admin key question to Rich.

## Verified constraints

- Production API is healthy and serving correctly. All verified endpoints return expected status codes.
- Auth-gated routes correctly return 401 without credentials — no security regression.
- Domain `buywhere.ai` is fully recovered. No lingering DNS or routing issues from the June 24 outage.
- Catalog stats are blocked by auth — need admin key routing from Rich to update milestone tracking.

## Decision

- Preserve today as an operations-memory note. The durable facts:
  - Transient cold-start blips are firing multiple monitor alerts per day — this is operational noise, not a trend.
  - Public surface is the reliable health truth. Railway control-plane access is inconsistent from this workspace.
  - Need admin API key to pull catalog stats and update Day-90 milestone tracking.

## Evidence

- `BUY-57335-domain-recovery-2026-06-25T1026Z.md`
- `BUY-57259-docs-2026-06-25T0635Z.md`
- `BUY-57498-resolution-2026-06-25T1837Z.md`
- `BUY-57495-incident-resolution-2026-06-25T1828Z.md`
- `BUY-57465-incident-resolution-2026-06-25T1724Z.md`
- `BUY-57436-resolution-2026-06-25T1644Z.md`
- `BUY-56870-incident-resolution-2026-06-24T1437Z.md`
