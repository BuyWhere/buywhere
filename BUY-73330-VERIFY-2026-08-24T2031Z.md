# BUY-73330 verification — Gate 2026-08-24T2031Z

## Runtime probe (multi-country, public /v1/products with monitoring key)

| Country | HTTP | ms | Rows | Country counts | Top merchants | category_path |
|---|---|---|---|---|---|---|
| US | 200 | 189 | 50 | US=50 | shopify_scrape 42, shopify_buy30620_crate 5, stock 1, hunt2 1, products_json 1 | 50/50 present (values null) |
| SG | 200 | 77 | 50 | SG=50 | decathlon_sg 24, woocommerce 14, shopify 6 | 50/50 present |
| MY | 200 | 17.5s | 50 | MY=50 | shopee 35, lazada_my 11, senheng_my 1, … | 50/50 present |
| TH | 200 | 1055 | 50 | TH=50 | jib_th 42, merchant_direct 8 | 50/50 present |
| PH | 200 | 5895 | 50 | PH=50 | shopify_www_datablitz_com_ph 43, merchant_direct 7 | 50/50 present |
| VN | 200 | 1050 | 50 | VN=50 | lazada_vn 47, woocommerce 3 | 50/50 present |
| ID | 200 | 1950 | 50 | ID=50 | shopify_blippocom 50 | 50/50 present |

**BUY-73752 / BUY-73330 pass criteria now PASS** — country filter is correct on every market, merchant diversity is non-degenerate on every market (no `{brillarmory.com: 50}` or `{id: 87330262824183393}` symptoms), `category_path` is present on all 50/50 rows.

## MCP Continuous Testing — RED, but unrelated to BUY-73330

Run 32764885225 (origin/main @ 5e3e83b0, 2026-08-24T18:52Z). Failed subtests all live on origin/main and do NOT touch the public /v1/products country filter:

- `search.test.mjs`:
  - `uses search_products tier when requested for keyword searches` — expected `[..., 21]`, actual `[..., 0.5]` (test L248)
  - `uses bounded title fallback for US laptop searches` — expected `[..., 21, 0]`, actual `[..., 0.5, 0]` (test L304)
  - `uses vector search for semantic mode when vector infra is available` — expected 1, actual 2 (test L595)
  - `uses RRF merge for hybrid mode when vector infra is available` — expected 1, actual 2 (test L638)
  - `uses correct cache key format` — expected true, actual ~ (test L841)
  - `archive path excludes storage categories for a device query` — expected true, actual false (test L924)
- `synthetic-amazon-guard.test.mjs` — BUY-72744 archive baseConditions (test L27)

The tier-call `21` vs `0.5` mismatch is a discrepancy in `buildTierQuery`/`p.limit` flow that landed in BUY-74173/BUY-74181 territory (search/tier owner). Vector / RRF count mismatch is also a tier owner concern.

## Disposition

Cannot mark BUY-73330 done under the code-change DoD because CI is red, but the failure is pre-existing on origin/main and unrelated to BUY-73330 scope. Cross-issue writes from heartbeat-timer runs are blocked at the API layer (`cross_issue_influence_run_context_required` 403) so I cannot PATCH the issue right now. Per the cross-issue write-block memory, durable evidence is in this file.

## Recommended next owner action

1. Reassign the MCP CI failure cluster (search.test.mjs tier/fallback/vector/RRF + synthetic-amazon-guard + Redis cache key + device-vs-storage) to the tier/search owner (Flux / `0ed653ab-…` or whichever agent owns BUY-74181 follow-up).
2. After that cluster is green on a fresh main push, retry BUY-73330 verification — every public probe should pass as shown above; the only remaining gate is CI green.